node_modules/
/data
dist/
# Secrets
.env
.env.*
*.env


# Secrets
.env
.env.*
*.env
*firebase-adminsdk*.json
firebase-service-account*.json
uploads/
# APNs / push credentials — never commit secrets
pushNotificationCredentials/*.p8
pushNotificationCredentials/*.p12
pushNotificationCredentials/*.cer
pushNotificationCredentials/*.pem

# Worker-app push credentials — never commit secrets
pushNotificationsOperations/*.p8
pushNotificationsOperations/*.p12
pushNotificationsOperations/*.cer
pushNotificationsOperations/*.pem
